const linkList = (arr) => {
  const proto = {
    addNode: (arg) => {},

    append: (arg) => {},

    preappend: (arg) => {},

    size: (arg) => {},

    tail: (arg) => {},

    at: (arg) => {},

    pop: (arg) => {},

    contains: (arg) => {},

    find: (arg) => {},

    toString: (arg) => {},
  };
  Object.assign.Object(proto);
};
const node = (data) => {};
